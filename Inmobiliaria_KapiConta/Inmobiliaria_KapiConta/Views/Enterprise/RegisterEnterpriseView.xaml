<UserControl x:Class="Inmobiliaria_KapiConta.Views.Enterprise.RegisterEnterpriseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="#2F4A5A">

    <UserControl.Resources>
        <!-- Estilos de los TextBox, Button y otros elementos permanecen iguales -->
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#1E1E1E"/>
            <Setter Property="Background" Value="#EEF2F5"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <Grid>
                                <TextBlock x:Name="ph" Text="{TemplateBinding Tag}" Foreground="#999999" FontSize="13" Margin="13,0,0,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="ph" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#1E8C8A"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E4EEF0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilos para los botones, conservando los diseños originales -->
        <Style x:Key="BtnBuscar" TargetType="Button">
            <Setter Property="Background" Value="#3A7BD5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Width" Value="90"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="22">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#2B5FAA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#1E4A8A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnRegistrar" TargetType="Button">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="22">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#1E8A4A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnVolver" TargetType="Button">
            <Setter Property="Background" Value="#E53935"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="22">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#C62828"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>


        <Grid Background="#2C1A0E">
            <Border Height="5" Background="#1E8C8A" VerticalAlignment="Bottom"/>
            <Border MaxWidth="500" Margin="40,30" Background="#3D2410" CornerRadius="20" Padding="32,28">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock Text="Registrar nueva empresa" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                        <!-- Sección búsqueda RUC -->
                        <Border Background="#4A2E16" CornerRadius="12" Padding="14,14" Margin="0,0,0,14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Style="{StaticResource InputBox}" Tag="RUC de la empresa" Text="{Binding NewEnterprise.Ruc, UpdateSourceTrigger=PropertyChanged}" MaxLength="11"/>
                                <Button Grid.Column="2" Style="{StaticResource BtnBuscar}" Content="Buscar" Height="44" VerticalAlignment="Bottom" 
                            Command="{Binding BtnBuscarCommand}" HorizontalAlignment="Center" Width="90"/>
                            </Grid>
                        </Border>

                        <!-- Sección resultados -->
                        <Border Background="#4A2E16" CornerRadius="12" Padding="14,14" Margin="0,0,0,24">
                            <StackPanel>
                                <TextBox Style="{StaticResource InputBox}" Tag="Razón Social" Text="{Binding NewEnterprise.Nombre, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,10" Foreground="#333333" MaxLength="100"/>
                                <TextBox Style="{StaticResource InputBox}" Tag="Dirección" Text="{Binding NewEnterprise.Direccion, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,10" Foreground="#333333" MaxLength="150"/>

                            </StackPanel>
                        </Border>

                        <!-- Mensaje de error -->
                        <TextBlock Text="{Binding MensajeError}" Foreground="#FF6B6B" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap" Visibility="{Binding HayError}"/>

                        <!-- Botones -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Style="{StaticResource BtnVolver}" Content="Volver" Command="{Binding VolverCommand}"/>
                            <Button Grid.Column="2" Style="{StaticResource BtnRegistrar}" Content="Registrar" Command="{Binding RegistrarCommand}" CommandParameter="{Binding newEnterprise}"/>
                        </Grid>
                    </StackPanel>

                    <!-- Logo -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Width="110">
                        <Image Source="/Assets/logoConCapibara.png" Width="90" Height="90" Stretch="Uniform" Margin="0,0,0,6"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
