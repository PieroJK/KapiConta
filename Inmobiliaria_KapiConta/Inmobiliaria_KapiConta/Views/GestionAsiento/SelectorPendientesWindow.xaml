<Window x:Class="Inmobiliaria_KapiConta.Views.GestionAsiento.SelectorPendientesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Inmobiliaria_KapiConta.Views.GestionAsiento"
        mc:Ignorable="d"
        Title="SelectorPendientesWindow"  Height="550" Width="1000"
        WindowStartupLocation="CenterOwner" Background="#2E4458">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="PENDIENTES POR MATAR" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

        <Border Grid.Row="1" Background="#3E5A73" CornerRadius="5" Padding="10" Margin="0,5,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🔍 BUSCAR:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                <TextBox x:Name="txtBuscar" Grid.Column="1" Height="30" VerticalContentAlignment="Center" Background="White" BorderThickness="0" 
                         Text="{Binding TextoBuscar, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </Border>

        <DataGrid x:Name="dgPendientes" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Extended" SelectionUnit="FullRow" Background="White" RowHeight="35"
                  ItemsSource="{Binding Pendientes}">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Header="Sel." Width="40"/>
                <DataGridTextColumn Header="Asiento" Binding="{Binding Asiento}" Width="100"/>
                <DataGridTextColumn Header="Cuenta" Binding="{Binding Cuenta}" Width="80"/>
                <DataGridTextColumn Header="Documento" Binding="{Binding Documento}" Width="120"/>
                <DataGridTextColumn Header="Proveedor" Binding="{Binding Proveedor}" Width="*"/>
                <DataGridTextColumn Header="Monto Original" Binding="{Binding MontoOriginal, StringFormat=N2}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Saldo" Binding="{Binding Saldo, StringFormat=N2}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="Red"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="CANCELAR" Width="120" Height="35" Margin="0,0,10,0" Background="#D2452D" Foreground="White" Command="{Binding CancelarCommand}"/>
            <Button Content="JALAR SELECCIONADOS" Width="200" Height="35" Background="#2BBF5A" Foreground="White" Command="{Binding AceptarCommand}"/>
        </StackPanel>
    </Grid>
</Window>
